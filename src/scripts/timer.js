export default class Timer
{
    constructor(seconds, endEvent)
    {
        this.seconds = seconds;
        this.endEvent = endEvent;

        this.timeout;
    }

    start()
    {
        
    }
}